spring:
  datasource:
    url: jdbc:postgresql:${DAE_DATASOURCE_URL://localhost:5406/disasterAndEmergency}
    username: brgy
    password: dae
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-version : 0

server:
  port: ${ports.dae}