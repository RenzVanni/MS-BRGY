spring:
  datasource:
    url: jdbc:postgresql:${DATASOURCE_PATH://localhost:5407}/healthAndSanitation
    username: brgy
    password: ${DATASOURCE_PASSWORD:has}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-version : 0

server:
  port: ${ports.has}