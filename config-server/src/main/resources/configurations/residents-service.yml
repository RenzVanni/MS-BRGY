spring:
  datasource:
    url: jdbc:postgresql:${DATASOURCE_URL://localhost:5439}/residents
    username: brgy
    password: ${DATASOURCE_PASSWORD:brgydb}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always


server:
  port: ${ports.residents}