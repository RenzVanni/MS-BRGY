spring:
  datasource:
    url: jdbc:postgresql:${DATASOURCE_URL://localhost:5405}/complaint
    username: brgy
    password: ${DATASOURCE_PASSWORD:complaint}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-version : 0

server:
  port: ${ports.complaint}