spring:
  datasource:
    url: jdbc:postgresql://localhost:5405/complaint
    username: brgy
    password: complaint
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-version : 0

server:
  port: ${ports.complaint}